import {Injectable, signal} from '@angular/core';
import {HttpClient} from '@angular/common/http';

@Injectable({
	providedIn: 'root'
})
export class AnitrackguessService {
  private baseUrl = 'http://localhost:3000';
	
}